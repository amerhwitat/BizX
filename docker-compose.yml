services:
  app:
    build: .
    image: amerhwitat/bizx:local
    container_name: bizx-app
    restart: unless-stopped
    ports:
      - "8000:8000"
      - "8080:8080"
    volumes:
      - bizx-data:/app/data
      - bizx-logs:/app/logs
    init: true
volumes:
  bizx-data:
  bizx-logs:
