# BizX Kotlin / Mobile

Kotlin implementation of the unified BizX feature contracts for JVM and Android/mobile targets. The same runtime surface covers game, NetworkUnified, launcher, InternetScanner, AssetBrowser, crypto, web, 3D, assets, store, rendering and scripts.

Platform UI and GPU backends remain adapters around the shared runtime.
