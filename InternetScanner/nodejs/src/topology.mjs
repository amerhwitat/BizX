export function buildTopology(project) { const nodes=[],edges=[]; const add=(id,type,label,data={})=>{if(!nodes.some(n=>n.id===id))nodes.push({id,type,label,...data});}; add('scanner','scanner','InternetScanner'); for(const h of project.hosts){const hid=`host:${h.ip}`;add(hid,'host',h.ip,{scope:h.scope});edges.push({source:'scanner',target:hid,evidence:'observed-by-scanner'});for(const p of h.openPorts??[]){const sid=`service:${h.ip}:${p.protocol}:${p.port}`;add(sid,'service',`${p.protocol}/${p.port}`,{service:p.service??'unknown',version:p.version??null});edges.push({source:hid,target:sid,evidence:'port-observation'});}} return {schema:1,nodes,edges}; }
