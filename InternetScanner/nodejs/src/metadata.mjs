export async function reverseDns(ip){const dns=await import('node:dns/promises');try{return await dns.reverse(ip)}catch{return []}}
export async function rdap(ip, timeoutMs=5000){const controller=new AbortController();const t=setTimeout(()=>controller.abort(),Math.min(timeoutMs,10000));try{const r=await fetch(`https://rdap.org/ip/${encodeURIComponent(ip)}`,{signal:controller.signal,headers:{accept:'application/rdap+json, application/json'}});if(!r.ok)return null;const d=await r.json();return {handle:d.handle??null,name:d.name??null,startAddress:d.startAddress??null,endAddress:d.endAddress??null,entities:(d.entities??[]).map(e=>e.handle).filter(Boolean).slice(0,32)}}catch{return null}finally{clearTimeout(t)}}
