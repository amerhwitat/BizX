# Web Implementations

BizX now has a standalone browser entry point and PHP service boundary under `web/`, alongside its existing JavaScript, TypeScript, Node.js, Java, Python and desktop implementations. Shared game state should remain deterministic and serializable so implementations can be tested against common fixtures.